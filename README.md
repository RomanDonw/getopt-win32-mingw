# POSIX getopt implementation for Windows (Win32) with MinGW.

Used sources from [getopt-win32](https://github.com/nanoporetech/getopt-win32) repo and setupped for MinGW static (lib\[...].a) \& dynamic (\[...].dll) library.